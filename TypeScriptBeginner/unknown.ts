// let inputData: unknown;
// inputData = 5;
// inputData = 'Max';
// let nameInput: string;
//  nameInput = inputData; //unknowj is not assignable to string but any YES

let inputData: unknown;
inputData = 5;
inputData = 'Max';
let nameInput: string;
if(inputData === 'string') {
    nameInput = inputData;
}